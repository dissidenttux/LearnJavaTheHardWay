class User {
  public static void Main (String args[]) {
    /* Recall that:
     * - A class is a blueprint of an object
     * - Each and every object has a "state" with is defined by the value of
     * "attributes" associated with the object...
     * - In Java, an object's attributes are defined by variables within a class
     */

    // attributes (of a User)
    String username = "user";
  }
}